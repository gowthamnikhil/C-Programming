#include<stdio.h>
int main()
{
    float l,b,per,ar;
    printf("Enter the lenght and breadth : ");
    scanf("%f %f",&l,&b);
    per=(l+b)*2;
    ar=l*b;
    if(pre>ar)
        printf("Perimeter is greater");
    else
        printf("Area is greater");
    return 0;
}